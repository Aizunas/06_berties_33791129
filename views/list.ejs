<!DOCTYPE html>
<html>
<head>
    <title>Book List <%=shopData.shopName%></title>
    <link rel="stylesheet"  type="text/css" href="/main.css" />
</head>
<body>
    <h1><%= shopData.shopName %></h1>
    <h2>List of Books</h2>
    <ul>
        <% availableBooks.forEach(function(book) { %>
            <li><%= book.name %> - £<%= book.price %></li>
        <% }); %>
    </ul>
    <a href="/">Home</a>
</body>
</html>